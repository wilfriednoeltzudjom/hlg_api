const accountRoles = require('./account-roles');
const accountStatuses = require('./account-statuses');
const genders = require('./genders');
const uploadedFileMediaTypes = require('./uploaded-file-media-types');
const sessionStatuses = require('./session-statuses');

module.exports = {
  accountRoles,
  accountStatuses,
  genders,
  uploadedFileMediaTypes,
  sessionStatuses,
};
